# server port
server:
  port: 8080
# database
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/game_playlist?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: user_codingrecipe
    password: 1234
  application:
    name: spring_test
# mybatis
mybatis:
  mapper-locations: classpath:mapper/member-mapper.xml
  config-location: classpath:mybatis-config.xml